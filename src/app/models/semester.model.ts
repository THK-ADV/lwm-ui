interface Semester {
    label: string
    abbreviation: string
    start: Date
    end: Date
    examStart: Date
    id: string
}

interface SemesterProtocol {
    label: string
    abbreviation: string
    start: string
    end: string
    examStart: string
}
