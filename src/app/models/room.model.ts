export interface Room {
    label: string
    description: string
    capacity: number
    id: string
}

export interface RoomProtocol {
    label: string
    description: string
    capacity: number
}
